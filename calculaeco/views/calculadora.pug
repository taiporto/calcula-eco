extends subLayout

block content

  - let i = 1;

    div.col-12.col-md-5.col-xl-4.center-box.center-box--vertical.center-box--calculadora
      div.center-box__rotated-background
      form#formCalculadora.form.center-box__form(class='form' action='/resultado' method='POST')
        fieldset.center-box__fieldset
          each d in disciplinas          
            if d.valido != false
              //- if d.nome_disciplina.length < 28
              div.form-group
                label.calc-label(for=''+d.nome_disciplina) #{d.nome_disciplina}
                input.inputs.form-control(type='number', step='0.1', min='0', max='10', placeholder='Nota', name=''+ i id=''+ i)
              //- else
              //-   label.pieces.calc-label--big(for=''+d.nome_disciplina) #{d.nome_disciplina}
              //-     input(type='number', step='0.1', min='0', max='10', class='inputs text-center form-control' placeholder='Insira sua nota', name=''+ i id=''+ i)
              - i = i+1;
        <br>
        button#buttonCalcula.btn.center-box__submit-btn Calcular!

    //- script.
    //-   if($(document).width() >= 768){
    //-     $(document).ready( function(){

    //-     let ww = $(window).width();

    //-     if($(window).width() <= 426){

    //-       var centro = $("#centroCalc").clone();
    //-       var instrucoes = $("#instrucoesCalc").clone();
    //-       $("#conteudoCalc").children().remove();
    //-       $("#conteudoCalc").append(centro);
    //-       $("#conteudoCalc").after("<div class='instrucoes container-fluid row' id='instrucoesContCalc' ></div>");
    //-       $( "#instrucoesContCalc" ).append(instrucoes);

    //-       $('.form-control').addClass("input-sm");
    //-     }

    //-     function createDivGroup(){
    //-       let inputs = $(".pieces");
    //-       let step = 2; // the step 

    //-       for (let i = 0; i < inputs.length; i += step) { // increment i with the step
    //-         inputs.slice(i, i + step + 1).wrapAll("<div class='inputGroup'></div>"); // slice for i to i + step + 1 and wrap
    //-         step++; // increment the step
    //-       }

    //-       let groupDivs = $('.inputGroup');

    //-       $('.inputGroup').wrapAll("<div class='row'></div>");

    //-       let lenGroup = groupDivs.length;

    //-       switch(lenGroup){
    //-         case 2:
    //-           for(let i = 0; i < lenGroup; i++) {
    //-             groupDivs[i].classList.add('col-sm-6', 'col-xs-12');
    //-           }
    //-           break;
    //-         case 3:
    //-           $('#centroCalc').attr('id', 'centroCalc3C');
    //-           $('.labelsCalc').addClass('labelsCalc3C');
    //-           $('.pieces').addClass('pieces3C');
    //-           $('.btn-calc').addClass('btn-calc-3C');
    //-           for(let i = 0; i < lenGroup; i++) {
    //-             groupDivs[i].classList.add('col-md-4');
    //-           }
    //-           break;
    //-       }
    //-     }

    //-     createDivGroup();

    //-       if($(".pieces").length >= 7){
    //-           console.log("entrou");
    //-           $("#formNotas").css('margin-top', '8%');
    //-           $(".btn-calc").css('margin-top', '-1%');
    //-         }

    //-     if($(window).size() < 768){
    //-       $('.inputs').addClass('input-lg');
    //-     }

    //-     function resizedw(){
    //-       if($(window).size() > 768){
    //-         $('.inputs').removeClass('input-lg');
    //-       }
    //-       else{
    //-         $('.inputs').addClass('input-lg');
    //-       }
    //-     }


    //-     window.onresize = function() {
    //-       let setter;
    //-       clearTimeout(setter);
    //-       setter = setTimeout(function() {
    //-         resizedw();
    //-       }, 100);
    //-     }
    //-   })
    //-   }




